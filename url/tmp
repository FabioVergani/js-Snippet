
var p,l=window.location,s=l.pathname;
if(s.length>1){
 l='/';
 if(s.endsWith(l)){
	//nofile
 }else{
	 if(l=s.lastIndexOf(l)){
		p=s;
		 console.log(1,p);
		s=s.substring(l+1);//filenameWithExtensionOrAncor
		p=p.substring(0,p.length-s.length);
		 
		console.log(2,p);
		 
		if(l=s.lastIndexOf('.')){
			s=s.substring(0,l);//filenameOnly
			if(l=/\d+$/.exec(s)){
				s.replace(l[0],'xxx');
				//console.dir(l)
				//l=l[0];
				//console.log(s);
			};

		};
	 };
 };
};

